name: dartframe
description: DartFrame is a flexible data manipulation library for Dart, inspired by pandas. It simplifies working with structured data like tables, CSVs, and JSON, offering intuitive tools for filtering, transforming, and analyzing data. Perfect for Dart and Flutter projects
version: 0.0.1
repository: https://github.com/gameticharles/DartFrame

environment:
  sdk: ^3.6.0

documentation: "https://github.com/gameticharles/DartFrame"

# Add regular dependencies here.
dependencies:
  advance_math: ^3.3.8
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
