name: dartframe
description: DartFrame is a Dart library inspired by pandas, simplifying structured data handling (tables, CSVs, JSON) with intuitive tools for filtering, transforming, and analysis in Dart/Flutter projects.
version: 0.1.1
repository: https://github.com/gameticharles/DartFrame

environment:
  sdk: ^3.6.0

documentation: "https://github.com/gameticharles/DartFrame"

# Add regular dependencies here.
dependencies:
  intl: ^0.20.1

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
